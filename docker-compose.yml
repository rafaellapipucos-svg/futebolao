services:
  bolao:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      DATA_DIR: /data
    volumes:
      - bolao-data:/data
    restart: unless-stopped

volumes:
  bolao-data:
